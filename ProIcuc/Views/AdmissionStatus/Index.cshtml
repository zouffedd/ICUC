@model IEnumerable<ProIcuc.Models.Applications.AdmissionStatus>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AdminStatusName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AdminStatusName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AdmissionStatusID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AdmissionStatusID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AdmissionStatusID })
        </td>
    </tr>
}

</table>
