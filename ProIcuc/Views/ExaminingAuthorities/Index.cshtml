@model IEnumerable<ProIcuc.Models.Applications.ExaminingAuthority>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ExaminingAutorityName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ExaminingAutorityName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ExaminingAuthorityID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ExaminingAuthorityID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ExaminingAuthorityID })
        </td>
    </tr>
}

</table>
