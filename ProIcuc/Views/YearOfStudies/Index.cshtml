@model IEnumerable<ProIcuc.Models.Examination.YearOfStudy>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.StudyYear)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.StudyYear)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.YearOfStudyID }) |
            @Html.ActionLink("Details", "Details", new { id=item.YearOfStudyID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.YearOfStudyID })
        </td>
    </tr>
}

</table>
